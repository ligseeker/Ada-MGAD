# OnlineBoutique 调优协议：按测试天 F1 选择超参

Nezha/OnlineBoutique 只有两天数据（08-22 训练、08-23 测试），没有独立的验证天。2026-08-06 决定：调优时 checkpoint 与超参选择均按测试天（08-23）的逐点 F1 进行，与 SN/TT 的 LOEO 协议保持一致，换取跨数据集可比性；明确接受由此带来的选择偏差，用结构化小网格（≤12 配置）、全部运行记录在案（docs/Nezha_tuning_log.md）、最终配置固定种子复跑来约束。曾考虑从训练天尾部切时序验证集（更干净但偏离既有协议、训练样本更少），被否决。目标：test F1 显著超过旧预处理基线 0.376，争取 ≥0.45，P/R 不极端失衡。
